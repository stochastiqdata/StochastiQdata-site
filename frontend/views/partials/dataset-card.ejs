<%
  // Fonction pour calculer la couleur du score
  function getScoreColor(score) {
    if (score >= 70) return '#14B8A6'; // Teal
    if (score >= 50) return '#3B82F6'; // Bleu
    if (score >= 30) return '#EAB308'; // Jaune
    return '#EF4444'; // Rouge
  }

  // Calcul du pourcentage pour le cercle
  const score = dataset.global_score || 0;
  const circumference = 2 * Math.PI * 36; // rayon = 36
  const strokeDasharray = circumference;
  const strokeDashoffset = circumference - (score / 100) * circumference;
  const scoreColor = getScoreColor(score);

  // Calcul des jours depuis la mise à jour
  const updatedAt = dataset.data_updated_at ? new Date(dataset.data_updated_at) : new Date(dataset.created_at);
  const daysAgo = Math.floor((new Date() - updatedAt) / (1000 * 60 * 60 * 24));

  // Modèle recommandé
  const recommendedModel = dataset.best_fit_models && dataset.best_fit_models.length > 0
    ? MODEL_LABELS[dataset.best_fit_models[0]] || dataset.best_fit_models[0]
    : 'N/A';
%>

<article class="card p-3 flex flex-col cursor-pointer group hover:scale-[1.02] transition-all duration-300" onclick="window.location.href='/modeling/<%= dataset.id %>'">

  <!-- Header: label + score badge -->
  <div class="flex items-center justify-between mb-1.5">
    <span class="text-xs font-semibold text-gray-400 dark:text-gray-500 uppercase tracking-wide">Dataset</span>
    <span class="text-xs font-bold px-2 py-0.5 rounded-full" style="background: <%= scoreColor %>22; color: <%= scoreColor %>">
      <%= score %><span class="font-normal opacity-70">/100</span>
    </span>
  </div>

  <!-- Titre -->
  <h3 class="text-sm font-bold text-gray-900 dark:text-white mb-2 group-hover:text-primary-500 transition-colors line-clamp-2 leading-snug">
    <%= dataset.name %>
  </h3>

  <!-- Source badges -->
  <div class="flex flex-wrap items-center gap-1 mb-2">
    <span class="badge badge-<%= dataset.source %>">
      <% if (dataset.source === 'kaggle') { %>
        <svg class="w-3 h-3" viewBox="0 0 24 24" fill="currentColor"><path d="M18.825 23.859c-.022.092-.117.141-.281.141h-3.139c-.187 0-.351-.082-.492-.248l-5.178-6.589-1.448 1.374v5.111c0 .235-.117.352-.351.352H5.505c-.236 0-.354-.117-.354-.352V.353c0-.233.118-.353.354-.353h2.431c.234 0 .351.12.351.353v14.343l6.203-6.272c.165-.165.33-.246.495-.246h3.239c.144 0 .236.06.285.18.046.149.034.255-.036.315l-6.555 6.344 6.836 8.507c.095.104.117.208.071.358z"/></svg>
      <% } else if (dataset.source === 'insee') { %>
        <svg class="w-3 h-3" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="12" r="10"/></svg>
      <% } %>
      <%= SOURCE_LABELS[dataset.source] || dataset.source %>
    </span>
    <% if (dataset.tags && dataset.tags.includes('iard')) { %>
      <span class="badge badge-iard">
        <svg class="w-3 h-3" viewBox="0 0 24 24" fill="currentColor"><rect x="3" y="3" width="18" height="18" rx="2"/></svg>
        IARD
      </span>
    <% } %>
    <% if (dataset.file_url) { %>
      <span class="badge badge-opendata">
        <svg class="w-3 h-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
        Disponible
      </span>
    <% } %>
  </div>

  <!-- Tags métier -->
  <div class="flex flex-wrap gap-1 mb-2">
    <% (dataset.tags || []).slice(0, 4).forEach(tag => { %>
      <span class="tag tag-<%= tag %>"><%= TAG_LABELS[tag] || tag %></span>
    <% }); %>
  </div>

  <!-- Infos + Recommandé -->
  <div class="flex items-center justify-between text-xs text-gray-500 dark:text-gray-400 mt-auto mb-2">
    <div class="flex items-center gap-2">
      <span><%= dataset.review_count || 0 %> avis</span>
      <% if (dataset.file_url) { %><span><%= (dataset.download_count || 0).toLocaleString('fr-FR') %> DL</span><% } %>
      <span><%= daysAgo === 0 ? 'Aujourd\'hui' : 'Il y a ' + daysAgo + 'j' %></span>
    </div>
    <span class="text-primary-500 font-semibold truncate ml-1"><%= recommendedModel %></span>
  </div>

  <!-- Bouton principal -->
  <% if (dataset.file_url) { %>
  <button onclick="event.stopPropagation(); cardTrackAndDownload('<%= dataset.id %>', '<%= dataset.file_url %>', this)"
     class="flex items-center justify-center gap-1.5 w-full py-2 px-3 bg-gradient-to-r from-primary-500 to-teal-500 hover:from-primary-600 hover:to-teal-600 text-white text-sm font-semibold rounded-lg transition-all duration-300 shadow hover:shadow-md">
    <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
      <polyline points="7 10 12 15 17 10"/>
      <line x1="12" y1="15" x2="12" y2="3"/>
    </svg>
    Télécharger<span class="opacity-70"><%= dataset.file_size_mb ? ' · ' + dataset.file_size_mb + ' MB' : '' %></span>
  </button>
  <% } else { %>
  <a href="/modeling/<%= dataset.id %>"
     class="flex items-center justify-center gap-1.5 w-full py-2 px-3 bg-gray-100 dark:bg-navy-700 hover:bg-gray-200 dark:hover:bg-navy-600 text-gray-700 dark:text-gray-200 text-sm font-semibold rounded-lg transition-all duration-300"
     onclick="event.stopPropagation()">
    <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
      <polyline points="14 2 14 8 20 8"/>
    </svg>
    Voir la fiche
  </a>
  <% } %>

  <!-- Actions secondaires (hover) -->
  <div class="flex gap-1.5 mt-2 pt-2 border-t border-gray-100 dark:border-navy-700 overflow-hidden max-h-0 group-hover:max-h-12 transition-all duration-200">
    <a href="/modeling/<%= dataset.id %>" class="btn-secondary flex-1 text-center text-xs py-1.5" onclick="event.stopPropagation()">Voir détails</a>
    <button class="btn-primary flex-1 text-xs py-1.5" onclick="event.stopPropagation(); openRatingModal('<%= dataset.id %>')">Noter</button>
  </div>
</article>

<script>
if (!window.cardTrackAndDownload) {
  window.cardTrackAndDownload = async function(datasetId, fallbackUrl, btn) {
    try {
      const res = await fetch(`/api/datasets/${datasetId}/download`, { method: 'POST' });
      const data = await res.json();
      const url = data.file_url || fallbackUrl;
      const a = document.createElement('a');
      a.href = url;
      a.download = '';
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
    } catch {
      window.location.href = fallbackUrl;
    }
  };
}
</script>
